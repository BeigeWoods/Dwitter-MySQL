version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/server

permissions:
  - object: /home/ubuntu/server
    pattern: "**"
    except: [".env"]
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: script/execute.sh
      timeout: 300
